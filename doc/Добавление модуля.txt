≈сли нужно добавить модуль:
1. ƒобавить в файл conf/modules данные:
		'footer2'	=>
			[	
				'link'		=>	'layouts/footer2',
				'class'		=>	'false',
				'file'		=>	'true'
			]
	где 
		footer2		- 	им€ модул€ и переменной котору нужно будет вставить в общем ключевом слое
		link		-	ссылка на файл шаблок (находитс€ в каталоге core/class/views/layouts/)
		class		-	класс в папке core/class/mod/modCLASSNAME √де класснейм будет footer2 (так же как и название модул€) при выставлении данного параметра
						ищет этот клас и запускаем метод run() (который должен выставить свойсво App::gi()->$modules['чего то там'] после этого чего то там выставитс€ в отображении )
		file		-	может быть только один (если он установлен то €вл€етс€ слоем по умолчанию который загружаетс€ в последний момент и отрисовывает все переменные)

2.	¬ случае необходимости содаетс€ класс с методом run() в папке core/class/base/ где чего то там обрабатываетс€
	class Footer2 extends Singleton{
	!!! ¬ момент выполнени€ метода отображение на экран полностью отключено

3.	—оздаетс€ отображение в папке core/class/views/layouts/